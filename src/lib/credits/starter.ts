// What a new account starts with, in tenths. One source of truth: the ledger grants it and
// the UI quotes it, so no screen can claim a number the grant doesn't match.
export const STARTER_CREDITS_TENTHS = 1000;
